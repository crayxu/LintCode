/**
 * @param size: An integer
 * @return: An integer list
 */
const generate = function (size) {
    var res = [];
    for (let i = 1; i < size+1; i++) {
        res.push(i);
    }
    return res;
}
